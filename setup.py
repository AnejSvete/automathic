from setuptools import setup

install_requires = [
    "pytest",
]


setup(
    name="automathic",
    install_requires=install_requires,
    version="0.1",
    scripts=[],
    packages=["automathic"],
)
